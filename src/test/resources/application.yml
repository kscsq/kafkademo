spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: fedordmitriev
test:
  topic: embedded-test-topic

monitor:
  kafka:
    bootstrap:
      config: "PLAINTEXT://localhost:9085"
    consumer:
      groupid: "fedordmitriev"